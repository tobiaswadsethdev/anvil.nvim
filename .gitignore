# ── Compiled Lua ─────────────────────────────────────────────────────────────
luac.out

# ── luarocks ─────────────────────────────────────────────────────────────────
*.src.rock
*.zip
*.tar.gz

# ── Go build artefacts ────────────────────────────────────────────────────────
# Built binaries (produced by `make build`)
bin/jira-anvil
bin/azdo-anvil
cmd/jira-anvil/jira-anvil
cmd/azdo-anvil/azdo-anvil

# Go test cache / coverage
*.test
*.out
coverage.txt

# ── Editor / IDE ──────────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Secrets ───────────────────────────────────────────────────────────────────
# Never commit config files containing API tokens
# ~/.config/anvil/config.yaml is outside the repo anyway,
# but guard against accidental copies:
config.yaml
*.token
.env
.env.*
